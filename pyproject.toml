[project]
name = "ml-basics"
version = "0.1.0"
description = "Notebooks of ML Basics"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "ipykernel",
    "matplotlib",
    "scipy",
    "torch",
    "gpytorch",
]
